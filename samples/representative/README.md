# Representative samples

The benchmark selected representative case IDs in the report, but no audio bytes are committed. Re-run the generator to recreate them under `tmp/audio/`; consult the applicable engine/model license before sharing any generated file.

Suggested inspection cases: `n01`, `e01`, `p01`, `p06`, `l01`.
